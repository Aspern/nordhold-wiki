# Web Application

This module will contain the Nordhold Wiki web application and its tests.

The application is static: all wiki content is stored in version-controlled
JSON files and included in the build. It must not depend on a database or a
runtime content-management service.

The frontend stack, JSON schemas, information architecture, accessibility
requirements, and build commands will be established by an accepted Spec Kit
feature.
